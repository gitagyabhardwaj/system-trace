{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window. Local-first: only the dialog plugin (save/open for export/import) and core window/event permissions. No network or shell access for activity data.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "dialog:allow-open",
    "notification:default",
    "wdio:default"
  ]
}
